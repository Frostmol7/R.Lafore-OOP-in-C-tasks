#include <iostream>
#include <iomanip>
using namespace std;
int main()
{
	for (int i = 1; i < 40; i++)
	{
		if (1 == i % 2) 
		{
			for (int j = 40; j > i / 2; j--)
				cout << " ";
			cout << setw(i) << setfill('X') << "X" << endl;
		}
	}
	return 0;
}
